#!/usr/bin/env tsx
/**
 * check-encryption-primitives.ts — PLACEHOLDER (activates Phase 04)
 *
 * Will enforce:
 * - No raw calls to Node crypto encrypt/decrypt outside packages/vault/.
 * - Only approved primitives (AES-256-GCM, argon2id) are used.
 *
 * Phase 01 has no encryption. Activates Phase 04.
 */
console.error('[check-encryption-primitives] PLACEHOLDER — activates Phase 04. OK');
