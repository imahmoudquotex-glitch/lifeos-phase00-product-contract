#!/usr/bin/env tsx
/**
 * check-rls.ts — PLACEHOLDER (activates Phase 02)
 *
 * Will enforce:
 * - Every table migration in migrations/ that creates a multi-tenant table
 *   has a matching RLS ENABLE and at least one POLICY statement.
 *
 * Phase 01 has no migrations. This guard exits 0 until Phase 02.
 */
console.error('[check-rls] PLACEHOLDER — activates Phase 02. OK');
