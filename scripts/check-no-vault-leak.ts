#!/usr/bin/env tsx
/**
 * check-no-vault-leak.ts — PLACEHOLDER (activates Phase 04)
 *
 * Will enforce:
 * - Vault-encrypted fields are never logged or returned raw in API responses.
 * - VAULT_KDF_PEPPER is never read outside packages/vault/.
 *
 * Phase 01 has no vault. Activates Phase 04.
 */
console.error('[check-no-vault-leak] PLACEHOLDER — activates Phase 04. OK');
